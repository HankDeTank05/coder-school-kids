import random as r # this is importing random with the psuedo name r

rand_num = r.randint(0, 255) # we are declaring a variable that is a random number using r.randint
print(rand_num) #prints the var, which is a number between 0 and 255